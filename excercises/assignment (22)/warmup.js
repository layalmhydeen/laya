
var readlineSync = require("readline-sync");




var asc=function(){
    var ask = require('readline-sync');  
var input = ask.question('What phrase would you like to encrypt? ').toLowerCase();  
var shift = parseInt(ask.question('How many letters would you like to shift? '));
    var sentence2=[];
    var code=0;
    for(var i=0;i<input.length;i++){
        
        if(input[i].code==41){
            var sen=inut[i].code+shift;
            sentence2[i].push(charCodeAt[sen]);
        }
        else   if(input[i].code==42){
            var sen=inut[i].code+shift;
            sentence2[i].push(charCodeAt[sen]);
        }   else   if(input[i].code==43){
            var sen=inut[i].code+shift;
            sentence2[i].push(charCodeAt[sen]);
        }   else   if(input[i].code==44){
            var sen=inut[i].code+shift;
            sentence2[i].push(charCodeAt[sen]);
        }   else   if(input[i].code==45){
            var sen=inut[i].code+shift;
            sentence2[i].push(charCodeAt[sen]);
        }   else   if(input[i].code==46){
            var sen=inut[i].code+shift;
            sentence2[i].push(charCodeAt[sen]);
        }  else   if(input[i].code==47){
            var sen=inut[i].code+shift;
            sentence2[i].push(charCodeAt[sen]);
        }   else   if(input[i].code==48){
            var sen=inut[i].code+shift;
            sentence2[i].push(charCodeAt[sen]);
        }  else   if(input[i].code==49){
            var sen=inut[i].code+shift;
            sentence2[i].push(charCodeAt[sen]);
        }  else   if(input[i].code==50){
            var sen=inut[i].code+shift;
            sentence2[i].push(charCodeAt[sen]);
        }  else   if(input[i].code==51){
            var sen=inut[i].code+shift;
            sentence2[i].push(charCodeAt[sen]);
        }   else   if(input[i].code==52){
            var sen=inut[i].code+shift;
            sentence2[i].push(charCodeAt[sen]);
        }  else   if(input[i].code==53){
            var sen=inut[i].code+shift;
            sentence2[i].push(charCodeAt[sen]);
        } else   if(input[i].code==54){
            var sen=inut[i].code+shift;
            sentence2[i].push(charCodeAt[sen]);
        } else   if(input[i].code==56){
            var sen=inut[i].code+shift;
            sentence2[i].push(charCodeAt[sen]);
        }
    }
}